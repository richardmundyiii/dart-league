export default function Venue() {
  return (
    <main>
      <section>
        <label>Team Name</label>
        <select>
          <option>Team A</option>
        </select>
      </section>
    </main>
  );
}
